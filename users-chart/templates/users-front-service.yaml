apiVersion: v1
kind: Service
metadata:
  name: users-front
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: users-front
  ports:
    - port: {{ .Values.usersFront.servicePort }}        # Port interne du Service
      targetPort: {{ .Values.usersFront.containerPort }}  # Port du container (Nginx)
  type: ClusterIP